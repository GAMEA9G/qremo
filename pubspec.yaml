name: qremo
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  cupertino_icons: ^1.0.8
  flutter_svg_provider: ^1.0.7
  flutter_svg: ^2.0.17
  pie_chart: ^5.4.0
  sleek_circular_slider: ^2.0.1
  provider: ^6.1.4
  image_size_getter_http_input: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - asset/
    
